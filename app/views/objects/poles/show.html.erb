<h2 class="page-header">6-10კვ საყრდენი #<%= @pole.name %></h2>

<table class="table table-bordered table-hover">
  <tr>
    <td class="text-muted" width="150">6-10კვ საყრდენის #</td>
    <td>
      6-10კვ საყრდენის #<code><%= @pole.name %></code>
      <% if @pole.number2.present? %>
        <span class="text-muted">პირობითი ნომერი: <code><%= @pole.number2 %></code></span>
      <% end %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">ტიპი</td>
    <td><%= @pole.pole_type_s %></td>
  </tr>
  <tr>
    <td class="text-muted">სიმაღლე</td>
    <td><code><%= number_with_precision @pole.height, precision: 2 %></code> მეტრი</td>
  </tr>
  <tr>
    <td class="text-muted">ვერტიკალური მდგომარეობა</td>
    <td>
      <%= @pole.vertical_position_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">ცვეთის ხარისხი</td>
    <td>
      <%= @pole.oldness_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">გამოსატანია</td>
    <td>
      <%= @pole.should_be_out_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">ტრავერსის ტიპი</td>
    <td>
      <%= @pole.traverse_type_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">იზოლატორის ტიპი</td>
    <td>
      <%= @pole.isolation_type_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">გამთიშველი</td>
    <td>
      <%= @pole.switch_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">გამთიშველის ტიპი</td>
    <td>
      <%= @pole.switch_type_s %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">შენიშვნა</td>
    <td><%= @pole.description%></td>
  </tr>
  <tr>
    <td class="text-muted">6-10კვ ფიდერი</td>
    <td>
      <% if @pole.fider %>
        <%= link_to @pole.fider_name, objects_fider_url(@pole.fider, tab: 'poles') %>
      <% end %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">ქვესადგური</td>
    <td>
      <% if @pole.substation %>
          <%= link_to @pole.substation_name, objects_substation_url(@pole.substation, tab: 'poles') %>
      <% end %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">მუნიციპალიტეტი</td>
    <td>
      <% if @pole.region %>
          <%= link_to @pole.region_name, region_url(@pole.region, tab: 'poles') %>
      <% end %>
    </td>
  </tr>
  <tr>
    <td class="text-muted">კოორდინატები</td>
    <td>
      E: <code><%= @pole.easting %></code>,
      N: <code><%= @pole.northing %></code>
    </td>
  </tr>
</table>

<h3 class="page-header">
  6-10კვ საყრდენის მდებარეობა
</h3>
<%= render partial: 'data/points_map', locals: { points: [ @pole.location ] } %>
